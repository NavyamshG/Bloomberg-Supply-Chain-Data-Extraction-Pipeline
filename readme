This script is expected to be run at bloomberg terminal with dual monitor setup.

Prereqs:
1. Download VSCode
2. Download Anaconda
3. Open VSCode from Anaconda
4. Open the code folder in VSCode
5. Install required python modules using "pip install -r requirements.txt" command
6. run the code with "python main.py"
7. Now, manually do below steps for each of tickers assigned to you
7A. Manually find and open the summary page of ticker
7B. If ticker not found, go to next ticker and start from step 7A
7C. If the ticker is found, open summary page, and give ticker name (without US Equity) to the code in console IO.
7B. wait until script captures all the screenshots of the ticker
7D. Proceed with next ticker